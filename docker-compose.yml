services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      SQLITE_PATH: /app/data/db.sqlite3
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1
      DJANGO_DEBUG: "1"
    volumes:
      - groupf_data:/app/data

volumes:
  groupf_data:
